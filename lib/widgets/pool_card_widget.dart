import 'package:flutter/cupertino.dart';

class PoolCardWidget extends StatelessWidget {
  const PoolCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}